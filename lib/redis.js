/*var	System = getModelFile("system"),
	systemModel = new System(),
	redisInfo = systemModel.getredisInfo(),


	
var redis = require('redis')
	client = redis.createClient(db_port, db_host);
module.exports = client;*/